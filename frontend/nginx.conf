server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This line ensures React Router handles the navigation
        try_files $uri $uri/ /index.html;
    }
}